language: python
sudo: false
python:
    - 2.7
    - py34
    - py35
    - py36
install:
    - pip install -U pip setuptools
    - pip install -U -e .[test]
script:
    - zope-testrunner --test-path=src {posargs:-vc}
notifications:
    email: false
cache: pip
